# Todo API (Postman only)

Small beginner-friendly FastAPI project meant to be tested with Postman.

## Setup
```bash
python -m venv .venv
source .venv/Scripts/activate
pip install -r requirements.txt
```

### Run API
```bash
uvicorn app.main:app --reload
```
